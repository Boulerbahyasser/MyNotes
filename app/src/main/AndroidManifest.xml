<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:label="@string/MyNotes"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name="com.example.myapplication.ui.FullscreenPhotoActivity" />
        <activity android:name="com.example.myapplication.ui.PhotoGalleryActivity" />
        <activity android:name="com.example.myapplication.ui.PhotoCaptureActivity" />
        <activity android:name="com.example.myapplication.ui.DetailsNoteActivity" />
        <activity android:name="com.example.myapplication.ui.AddNoteActivity" />

        <activity
            android:name="com.example.myapplication.ui.NoteListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FileProvider pour partager la photo avec l’app Caméra -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
